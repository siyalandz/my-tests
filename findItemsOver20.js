function findItemsOver20(items){
    var listOver20 = [];
     for(var i=0;i<items.length; i++){
       var listItem = items[i];
       if(listItem.qty>20){
         listOver20.push(listItem)
         // var fruit = items[key]
            //list.push(fruit)
                   } 
          }         
     //console.log(list)
       return listOver20 ;
      };